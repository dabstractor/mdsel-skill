{
  "name": "mdsel-opencode-plugin",
  "version": "1.0.0",
  "description": "OpenCode plugin that reminds agents to use mdsel instead of Read for large Markdown files",
  "type": "module",
  "main": "./mdsel-reminder.ts",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit"
  },
  "keywords": [
    "opencode",
    "plugin",
    "mdsel",
    "markdown"
  ],
  "dependencies": {
    "@opencode-ai/plugin": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "latest"
  }
}
